- 현재 : 경로나 레이아웃을 아예 통째로 분리하는 방식
- `멀티 레이아웃(Multi-layout) 구조]` 또는 **[도메인/서비스 격리(Isolation)]
- RBAC(Role-Based Access Control, 역할 기반 접근 제어)
- :껍데기(UI 구조)는 같지만 알맹이(데이터와 기능)가 권한에 따라 달라지는 방식 -> 보완해야함 현재는 경로 기준으로 나눈 상태.
- 실무적인 관점”에서는 지금처럼 역할별로 껍데기(레이아웃·경로)를 나누는 구조가 더 일반적이고, “같은 껍데기 + 권한에 따라 알맹이만 다르게”는 역할이 많아지거나, “한 포털에서 역할만 바꿔 쓰는” 요구가 있을 때 보완용으로 가져가는 경우가 많습니다.
- `풀 스크린 포털`
- `신규 UI로 교체`